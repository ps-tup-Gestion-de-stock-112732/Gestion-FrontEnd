import { NombreEstadosEmpleadoPipe } from './nombre-estados-empleado.pipe';

describe('NombreEstadosEmpleadoPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreEstadosEmpleadoPipe();
    expect(pipe).toBeTruthy();
  });
});
