import { NombreEstadosPipe } from './nombre-estados.pipe';

describe('NombreEstadosPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreEstadosPipe();
    expect(pipe).toBeTruthy();
  });
});
