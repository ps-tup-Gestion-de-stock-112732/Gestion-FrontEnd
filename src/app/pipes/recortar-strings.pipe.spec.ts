import { RecortarStringsPipe } from './recortar-strings.pipe';

describe('RecortarStringsPipe', () => {
  it('create an instance', () => {
    const pipe = new RecortarStringsPipe();
    expect(pipe).toBeTruthy();
  });
});
